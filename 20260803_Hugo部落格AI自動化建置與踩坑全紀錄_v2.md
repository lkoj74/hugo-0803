---
title: 從零到一完整複製指南：Hugo個人部落格建置、客製化與GitHub Actions自動化雲端發布全紀錄 (v2)
date: 2026-08-03T15:45:00+08:00
tags: [Hugo, GitHub-Pages, GitHub-Actions, Dream主題, 入門複製手冊, 實戰指南, 專案紀錄]
---

# 🚀 從零到一 Hugo 個人部落格自動化建置與雲端發布完整複製指南 (v2)

> 💡 **本指南說明**：這是一份**「拿到就能 100% 複製」**的實戰手冊。無論是您自己未來要重蓋網站，或是分享給其他人，只要依序執行以下 10 個階段的指令與設定，即可從零搭建出包含 **Dream 炫麗主題、Giscus 留言板、WebP 圖片自動壓縮、動態訪客計數器** 以及 **GitHub Actions 30秒自動化雲端部署** 的專業個人部落格！

---

## 📋 系統流程概覽 (Architecture)

```
[本地電腦開發]                      [雲端自動化]                 [全世界造訪]
Markdown 文章 ➔ Git Commit ➔ Git Push ➔ GitHub Actions 雲端編譯 ➔ GitHub Pages (https://...)
```

---

## 🛠️ 第一階段：環境準備 (Prerequisites)

在開始前，請確保電腦已安裝以下基礎工具。開啟 **Windows PowerShell**（以管理員身份執行）：

### 1. 安裝 Hugo Extended (必須為 Extended 版以支援 Sass/CSS)
```powershell
winget install Hugo.Hugo.Extended
```
*驗證安裝*：
```powershell
hugo version
# 應顯示 hugo v0.164.0+extended ...
```

### 2. 安裝 Git 與 GitHub CLI (簡化 GitHub 授權)
```powershell
winget install Git.Git
winget install GitHub.cli
```
*驗證與登入 GitHub*：
```powershell
gh auth login
# 依照畫面指示選擇 GitHub.com -> HTTPS -> 瀏覽器登入授權
```

---

## 📁 第二階段：初始化 Hugo 專案目錄

切換到您要存放專案的硬碟目錄（例如 `J:\`）：

```powershell
# 1. 建立 Hugo 專案目錄（假設專案名為 my-blog）
cd "J:\"
hugo new site my-blog

# 2. 進入專案資料夾
cd my-blog
```

---

## 🔗 第三階段：初始化 Git 儲存庫與 GitHub 連結

```powershell
# 1. 初始化 Git
git init

# 2. 新增標準 .gitignore 檔案 (防止將編譯暫存檔傳上 git)
Set-Content -Path .gitignore -Value @"
public/
resources/_gen/
.hugo_build.lock
*.log
"@

# 3. 在 GitHub 建立公開 (Public) 儲存庫並連結
# (注意：GitHub Pages 免費版必須為 Public)
gh repo create my-blog --public --source=. --remote=origin --push
```

---

## 🎨 第四階段：下載主題與【關鍵避坑防護】

我們選用視覺極致炫麗的 **Dream** 主題：

```powershell
# 1. 下載 Dream 主題至 themes/dream
git clone --depth 1 https://github.com/g1eny0ung/hugo-theme-dream.git themes/dream

# 🚨🚨 2.【最關鍵避坑神操作 - 必做！】🚨🚨
# 必須刪除主題內部的 .git 隱藏資料夾！
# 否則主專案 Git 會將其誤判為空 Submodule，上傳至 GitHub 時會變成空資料夾，導致雲端編譯 404！
Remove-Item -Recurse -Force themes\dream\.git

# 3. 確保 Git 將主題內所有 121 個 HTML 模板當作一般檔案追蹤
git rm --cached themes/dream -ErrorAction SilentlyContinue
git add themes/dream
```

---

## ⚙️ 第五階段：配置核心設定檔 `hugo.toml`

覆蓋專案根目錄下的 [`hugo.toml`](file:///j:/【0803】google%20AntiGravity%202.0-20260606/hugo.toml)：

```toml
# 將 baseURL 換成您的 GitHub Pages 網址 (語法: https://<GitHub帳號>.github.io/<專案名稱>/)
baseURL = "https://lkoj74.github.io/hugo-0803/"
locale = "zh-tw"
title = "我的 Dream 個人部落格"
theme = "dream"

# 強制首頁只輸出 HTML，防止生成 index.xml 導致瀏覽器誤載 RSS
[outputs]
  home = ["HTML"]

[params]
  author = "部落格作者"
  avatar = "img/author.png"
  headerTitle = "我的個人部落格"
  motto = "🚀 分享技術隨筆、AI 開發與生活想法"
  description = "歡迎來到我的個人部落格！專注於技術筆記、AI 開發與日常生活分享。"
  keywords = ["Hugo", "Tailwind", "AI", "網頁開發", "部落格"]

  # Dream 主題配色 (亮色: emerald / 暗色: forest)
  lightTheme = "emerald"
  darkTheme = "forest"

  stickyNav = true
  showTableOfContents = true
  showSummaryCoverInPost = true

  # Giscus 留言板設定 (在 https://giscus.app/zh-TW 取得您的 Repo ID 與 Category ID)
  [params.giscus]
    enabled = true
    repo = "lkoj74/hugo-0803"
    repoId = "R_kgDOXXXXXXXX"
    category = "Announcements"
    categoryId = "DIC_kwDOXXXXXXXX"

[menu]
  [[menu.main]]
    identifier = "posts"
    name = "📝 文章筆記"
    url = "posts/"
    weight = 10

  [[menu.main]]
    identifier = "tags"
    name = "🏷️ 標籤分類"
    url = "tags/"
    weight = 20

  [[menu.main]]
    identifier = "about"
    name = "👤 關於本站"
    url = "about/"
    weight = 30
```

---

## 🧩 第六階段：客製化 Layout 與進階功能注入

在根目錄建立 `layouts/` 覆蓋層以保護大頭貼、注入訪客計數器與 WebP Pipeline：

### 1. 客製化導覽列 (`layouts/partials/nav.html`)
建立 `layouts/partials/nav.html`，在大頭貼加上 **`48px × 48px` 與 `flex-shrink-0` 鎖定** 防止展屏，並在搜尋按鈕右側嵌入**訪客動態計數器**：

```gotemplate
<nav x-data="{ isSticky: false }" x-init="window.addEventListener('scroll', () => { isSticky = window.scrollY > 30 })" class="sticky top-0 z-30 mt-4 lg:mt-8 py-4" :class="{ 'bg-base-100 shadow-lg dark:border-b dark:border-base-content/30': isSticky }">
  <div class="container flex justify-between px-4">
    <!-- 左側 Profile：48px 大頭貼鎖定 + 標題與標語 -->
    <section class="flex items-center gap-3">
      <div class="flex-shrink-0" style="width: 48px; height: 48px;">
        <div class="rounded-full overflow-hidden shadow-sm ring-1 ring-primary/20" style="width: 48px; height: 48px;">
          <img src="{{ site.Params.avatar | relURL }}" alt="{{ site.Params.headerTitle }}" class="rounded-full" style="width: 48px; height: 48px; object-fit: cover;" />
        </div>
      </div>
      <div class="flex flex-col justify-center">
        <a href="{{ site.BaseURL }}" class="text-xl font-bold tracking-tight cursor-pointer hover:text-primary transition-colors">
          {{ site.Params.headerTitle }}
        </a>
        {{ if site.Params.motto }}
        <div class="text-base-content/70 text-sm mt-0.5">{{ site.Params.motto }}</div>
        {{ end }}
      </div>
    </section>

    <!-- 右側選單與動態計數器 -->
    <section class="hidden sm:flex sm:items-center sm:gap-2 md:gap-4">
      {{ range site.Menus.main }}
      <a class="inline-flex items-center p-2 cursor-pointer hover:font-bold font-semibold text-base-content/70 hover:text-base-content transition-all" href="{{ .URL | relURL }}">{{ .Name }}</a>
      {{ end }}
      
      <!-- 訪客動態計數器 -->
      <div class="inline-flex items-center ml-2">
        <nobr><table border="0" cellspacing="0" cellpadding="0"><tbody><tr><td><a href="https://www.stylemap.co.jp/counter/taiwan/"><img src="https://www.f-counter.net/ani1/67/1785730376/" alt="網站計數器" border="0" style="margin:0px; padding:0px; border:0px; vertical-align:bottom"></a></td>
        <td><a href="https://www.stylemap.co.jp/counter/taiwan/"><img src="https://www.f-counter.net/ani2/67/1785730376/" alt="網站計數器" border="0" style="margin:0px; padding:0px; border:0px; vertical-align:bottom"></a></td></tr></tbody></table></nobr>
      </div>
    </section>
  </div>
</nav>
```

### 2. 圖片自動 WebP 壓縮與懶加載 Hook (`layouts/_default/_markup/render-image.html`)
建立 `layouts/_default/_markup/render-image.html`：

```gotemplate
{{- $img := .Page.Resources.GetMatch .Destination -}}
{{- if $img -}}
  {{- $webp := $img.Resize "1200x webp q85" -}}
  <figure class="my-4">
    <img src="{{ $webp.RelPermalink }}" alt="{{ .Text }}" width="{{ $webp.Width }}" height="{{ $webp.Height }}" loading="lazy" class="rounded-lg shadow-md max-w-full h-auto" />
    {{ with .Title }}<figcaption class="text-center text-sm text-base-content/60 mt-2">{{ . }}</figcaption>{{ end }}
  </figure>
{{- else -}}
  <img src="{{ .Destination | safeURL }}" alt="{{ .Text }}" loading="lazy" class="rounded-lg shadow-md max-w-full h-auto" />
{{- end -}}
```

### 3. Giscus 留言板注入 (`layouts/partials/commentSystems.html`)
建立 `layouts/partials/commentSystems.html` 引入 Giscus：

```gotemplate
{{ if site.Params.giscus.enabled }}
<article class="mt-8">
  <script src="https://giscus.app/client.js"
          data-repo="{{ site.Params.giscus.repo }}"
          data-repo-id="{{ site.Params.giscus.repoId }}"
          data-category="{{ site.Params.giscus.category | default "Announcements" }}"
          data-category-id="{{ site.Params.giscus.categoryId }}"
          data-mapping="pathname"
          data-strict="0"
          data-reactions-enabled="1"
          data-emit-metadata="0"
          data-input-position="top"
          data-theme="preferred_color_scheme"
          data-lang="zh-TW"
          crossorigin="anonymous"
          async>
  </script>
</article>
{{ end }}
```

---

## ✍️ 第七階段：撰寫第一篇文章 (Page Bundles 圖片結構)

採用 **Page Bundles** 結構，將文章 Markdown 與圖片放在同一個資料夾：

```powershell
# 1. 建立文章資料夾
New-Item -ItemType Directory -Path "content/posts/my-first-post" -Force
```

建立 `content/posts/my-first-post/index.md`：

```markdown
---
title: "歡迎來到我的全新 Hugo 個人部落格！"
date: 2026-08-03T15:00:00+08:00
draft: false
tags: ["Hugo", "網頁設計", "心得分享"]
categories: ["技術分享"]
---

這是我使用 Hugo Extended 與 Dream 主題搭建的第一篇文章！

![封面圖片](featured.jpg)

### 文章特色：
1. **極速載入**：Hugo 靜態編譯 + WebP 85% 圖片自動壓縮。
2. **自動部署**：每一次 `git push` 後，雲端會自動更新上線。
```

*提示*：將一張測試圖片命名為 `featured.jpg` 放進 `content/posts/my-first-post/` 資料夾中。
同時將您的個人大頭貼放至 `static/img/author.png`。

---

## 🤖 第八階段：設定 GitHub Actions 雲端自動化發布腳本

在專案根目錄建立 `.github/workflows/hugo.yml`：

```yaml
name: Deploy Hugo site to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v3
        with:
          hugo-version: '0.164.0'
          extended: true

      - name: Build
        run: hugo --minify --baseURL "https://lkoj74.github.io/hugo-0803/"

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
```

---

## 🚀 第九階段：推送到 GitHub 並開啟 GitHub Pages

在 PowerShell 中執行全自動提交與推送：

```powershell
# 1. 在根目錄建立 .nojekyll 防止 GitHub 忽略底線資料夾
Set-Content -Path .nojekyll -Value "# Disable Jekyll"

# 2. 提交並推送到 GitHub main 分支
git add .
git commit -m "feat: complete initial Hugo site setup with automated deployment"
git push -u origin main
```

### 開啟 GitHub Pages（只需在網頁上設定一次）：
1. 開啟您的 GitHub 倉庫：`https://github.com/您的帳號/專案名稱`
2. 進入 **`Settings` ➔ `Pages`**。
3. 在 **`Build and deployment`** 區塊：
   - **Source** 改選為：**`Deploy from a branch`**
   - **Branch** 選擇：**`gh-pages`** ➔ 資料夾選 **`/ (root)`**。
   - 點擊右側 **`Save` (儲存)**。

約 **30 秒～ 1 分鐘後**，GitHub Actions 自動發布完成，全網造訪您的公開部落格：
👉 **`https://<您的帳號>.github.io/<專案名稱>/`**

---

## 📘 第十階段：建立 AI 專案說明書與 SOP (`AGENTS.md` & `SKILLS.md`)

在專案根目錄建立以下兩個檔案，讓 AI 助理未來能完全接管與維護：

### 1. `AGENTS.md` (給 AI 的專案規則書)
```markdown
# 🤖 AGENTS.md
- 語言與溝通：一律使用繁體中文 (Traditional Chinese, Taiwan)。
- 避坑防護：下載新主題時必須刪除 `themes/xxx/.git` 防止空 Submodule。
- 大頭貼規範：固定 `48px × 48px` 與 `flex-shrink-0` 鎖定。
- 部署方式：推送到 main 分支，經 peaceiris 部署至 gh-pages 分支。
```

### 2. `SKILLS.md` (常用工作流程手冊)
```markdown
# 📖 SKILLS.md
- 日常寫文章 SOP：在 `content/posts/` 建立資料夾 -> 放入 index.md 與圖片。
- 一鍵發布 SOP：`git add .` ➔ `git commit -m "feat: xxx"` ➔ `git push -u origin main`。
```

---

## 🏆 總結與維護對照表

| 階段 | 任務內容 | 關鍵防坑點 |
| :--- | :--- | :--- |
| 1~3 階段 | 環境安裝與 Git 儲存庫初始化 | 倉庫必須設為 `Public` |
| 4~5 階段 | 下載 Dream 主題與設定 `hugo.toml` | **務必刪除 `themes/dream/.git`** |
| 6~7 階段 | 48px大頭貼保護、計數器、WebP Pipeline | 使用 Page Bundles 資料夾結構 |
| 8~9 階段 | 配置 `hugo.yml` 與開啟 GitHub Pages | Source 選 `Deploy from a branch` (`gh-pages`) |
| 10 階段 | 寫入 `AGENTS.md` 與 `SKILLS.md` | 讓 AI 能夠完美接管日常維護 |
