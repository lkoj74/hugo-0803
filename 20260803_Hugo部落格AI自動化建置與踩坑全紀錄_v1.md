---
title: Hugo個人部落格建置與GitHub Actions全自動化發布踩坑全紀錄
date: 2026-08-03T15:25:00+08:00
tags: [Hugo, GitHub-Pages, GitHub-Actions, Dream主題, AI實戰, 專案紀錄]
---

# 🚀 Hugo 個人部落格 AI 自動化建置與踩坑全紀錄

## 一、 專案概覽與對話歷程

本專案為使用 **Hugo Extended** 搭配 **Dream** 炫麗主題，結合 **GitHub Actions** 與 **GitHub Pages** 打造的個人自動化部落格系統。

- **公開網址**：https://lkoj74.github.io/hugo-0803/
- **GitHub 倉庫**：https://github.com/lkoj74/hugo-0803.git
- **主要對話脈絡**：
  1. **初始需求**：診斷並安裝 Hugo Extended 擬定部落格建置方案。
  2. **主題探索與切換**：Blowfish ➔ Congo ➔ PaperMod ➔ Dream 主題。
  3. **客製化元件**：嵌入個人大頭貼 (48px 圓形保護)、訪客動態計數器、Giscus (GitHub Discussions) 留言板、WebP 圖片自動壓縮 Pipeline。
  4. **雲端自動化**：設定 GitHub Actions 實現每次 `git push` 後 30 秒自動發布上線。
  5. **專案規範建立**：建立 `AGENTS.md` (AI 規則書) 與 `SKILLS.md` (SOP 手冊)。

---

## 二、 專案建立的核心成果與檔案結構

### 1. 核心檔案與元件說明
- **`hugo.toml`**：設定 `theme = "dream"`、`locale = "zh-tw"`、Giscus 參數與 outputs 規則。
- **`layouts/partials/nav.html`**：客製化導覽列，嵌入訪客計數器並將個人大頭貼尺寸固定為 `48px × 48px` (帶 `flex-shrink-0` 鎖定)。
- **`layouts/partials/commentSystems.html`**：整合 Giscus 留言系統。
- **`layouts/_default/_markup/render-image.html`**：圖片 Pipeline Hook，自動將 Markdown 圖片轉換為 WebP 格式、進行 85% 品質壓縮與 lazy loading。
- **`.github/workflows/hugo.yml`**：使用 `peaceiris/actions-hugo` + `peaceiris/actions-gh-pages` 的 100% 必勝部署腳本。
- **`AGENTS.md`**：寫給 AI 助理的專案行為規範（繁體中文、防坑指示、檔名慣例）。
- **`SKILLS.md`**：發文、更換主題、預覽與發布的標準作業程序 (SOP)。
- **`README.md`**：專案架構與實戰宣言導覽。

---

## 三、 實戰踩坑全紀錄與解決方案 (Troubleshooting)

### 🚨 坑點 1：Git 指令出現 `fatal: not a git repository`
- **原因**：使用者在 PowerShell 操作時當前目錄位在 `C:\Users\lkoj74`，而非部落格專案根目錄。
- **解決方式**：先執行 `cd "J:\【0803】google AntiGravity 2.0-20260606"` 切換進入專案資料夾後再執行 Git 指令。

### 🚨 坑點 2：最致命大坑——主題資料夾變為空 Submodule 導致雲端 404
- **原因**：使用 `git clone` 下載 `themes/dream` 時，主題內包含了 `.git` 隱藏資料夾。主專案 Git 將其誤判定為 Submodule，上傳至 GitHub 時只傳了一個「空目錄連結」，121 個 HTML 模板全未傳上去。
- **解決方式**：
  1. 刪除主題內的 `.git` 資料夾：`Remove-Item -Recurse -Force themes\dream\.git`
  2. 解除 cached 狀態並重新 add：`git rm --cached themes/dream; git add themes/dream`
  3. 將全套 121 個模板檔案 100% commit 上傳。

### 🚨 坑點 3：首頁出現 XML 原始碼或陷入多語言跳轉
- **原因**：`hugo.toml` 中預設輸出了 `index.xml` RSS 檔案，且預設多語言子目錄 `zh-tw/` 造成瀏覽器抓取到 RSS 代碼。
- **解決方式**：在 `hugo.toml` 中將 `[outputs]` 設定為 `home = ["HTML"]`，強制作為純 HTML 首頁輸出。

### 🚨 坑點 4：GitHub Pages 從 Private 切換到 Public 的部署源鎖定
- **原因**：GitHub Pages 免費版要求倉庫必須為 Public。切換為 Public 後，GitHub 內部的 `build_type` 在 `workflow` 與 `branch` 之間出現註冊延遲。
- **解決方式**：使用 REST API 重新 POST/PUT 建立 Pages，並改用最穩定的 `gh-pages` 分支發布模式，避免 API 異步卡住。

### 🚨 坑點 5：Fastly CDN 邊緣伺服器強行快取 404 響應 (`X-Cache: HIT`)
- **原因**：在網頁尚未完全佈署成功前點擊了網址，全球 CDN 節點快取了 404 狀態（快取時間高達 190秒以上）。
- **解決方式**：發送一個空的 commit 清理 CDN，並引導使用者使用無痕視窗或 `Ctrl + F5` 繞過 CDN 舊快取。

---

## 四、 維護 SOP 懶人包

1. **下載新主題**：務必隨手刪除 `themes/主題/.git` 資料夾。
2. **日常寫文章**：在 `content/posts/` 建立資料夾，放入 `index.md` 與圖片。
3. **一鍵推送發布**：`git add .` ➔ `git commit -m "feat: xxx"` ➔ `git push -u origin main`。
